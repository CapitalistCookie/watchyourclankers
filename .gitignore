__pycache__/
*.pyc
.venv/
venv/
node_modules/
/data/
*.log
.DS_Store
.cookie_secret
.wyc_token
*.local
# Python packaging artifacts (M1 — setuptools). build/codemirror/ is the TRACKED CM
# vendoring recipe; only the setuptools output is ignored.
/build/lib/
/build/bdist.*
*.egg-info/
/dist/
